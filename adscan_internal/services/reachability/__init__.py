"""Reachability services package.

Hosts the shared building blocks for collector reachability gating, including
the single source of truth for the massdns hostname resolution report consumed
by both Phase 2 (collector DNS resolver) and Phase 3 (``cli/nmap``).
"""
